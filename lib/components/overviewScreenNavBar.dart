import 'package:flutter/material.dart';

class OverViewScreenNavBar extends StatelessWidget {
  const OverViewScreenNavBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Drawer();
  }
}
