import 'package:flutter/material.dart';

Container AuthCustomTextFeildHelper(Text text) {
  return Container(
    margin: EdgeInsets.symmetric(horizontal: 10, vertical: 0),
    alignment: Alignment.centerLeft,
    child: text,
  );
}
